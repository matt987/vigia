<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE struts-config PUBLIC
          "-//Apache Software Foundation//DTD Struts Configuration 1.3//EN"
          "http://jakarta.apache.org/struts/dtds/struts-config_1_3.dtd">

<!-- vigia app -->
<struts-config>
    <form-beans>
        <form-bean name="ClientesActionForm" type="com.vigia.struts.ClientesActionForm"/>
        <form-bean name="MarcasActionFormBean" type="com.vigia.struts.MarcasActionFormBean"/>
        <form-bean name="UsuariosActionFormBean" type="com.vigia.struts.UsuariosActionFormBean"/>
        <form-bean name="MarcasActionFormBean" type="com.vigia.struts.MarcasActionFormBean"/>
        <form-bean name="LoginActionFormBean" type="com.vigia.struts.LoginActionFormBean"/>
        <form-bean name="ClientesActionFormBean" type="com.vigia.struts.ClientesActionFormBean"/>
    
    </form-beans>
    
    <global-exceptions>
    
    </global-exceptions>

    <global-forwards>
        <forward name="logout" path="/Logout.do"/>
        <!--forward name="welcome"  path="/Welcome.do"/>
        <forward name="user"  path="/User.do"/-->
        <forward name="login"  path="/Login.do"/>
        <forward name="usuarios"  path="/Usuarios.do"/>
        <forward name="marcas"  path="/Marcas.do"/>
        <forward name="clientes"  path="/Cliesntes.do"/>
    </global-forwards>

    <action-mappings>
        <action input="/" name="LoginActionFormBean" path="/login" scope="session" type="com.vigia.struts.LoginAction" validate="false">
            <forward name="success" path="/sucess.jsp"/>
            <forward name="failure" path="/"/>
        </action>
        <action input="/usuarios-body.jsp" name="UsuariosActionFormBean" path="/usuarios" scope="session" type="com.vigia.struts.UsuariosAction" validate="false">
            <forward name="success" path="/usuarios.jsp"/>
            <forward name="failure" path="/usuarios.jsp"/>
          </action>  
        <action input="/marcas-body.jsp" name="MarcasActionFormBean" path="/marcas" scope="session" type="com.vigia.struts.MarcasAction" validate="false">
            <forward name="success" path="/marcas.jsp"/>
            <forward name="failure" path="/marcas.jsp"/>
          </action>
        <action parameter="action" input="/clientes-body.jsp" name="ClientesActionFormBean" path="/clientes" scope="session" type="com.vigia.struts.ClientesAction" validate="false">
            <forward name="buscar" path="/clientes" redirect="true"/>
          </action>        
         <!--action path="/Welcome" forward="/welcomeStruts.jsp"/-->
         
        <action path="/User" forward="/user-form.jsp"/>
        <action path="/Logout" forward="/logout.jsp"/>
        <action path="/Usuarios" forward="/usuarios.jsp"/>
        <action path="/Marcas" forward="/marcas.jsp"/>
        <action path="/clientes" forward="/clientes.jsp"/>
    </action-mappings>
    
    <controller processorClass="org.apache.struts.tiles.TilesRequestProcessor"/>

    <message-resources parameter="com/vigia/struts/ApplicationResource"/>    
    
    <!-- ========================= Tiles plugin ===============================-->
    <!--
    This plugin initialize Tiles definition factory. This later can takes some
    parameters explained here after. The plugin first read parameters from
    web.xml, thenoverload them with parameters defined here. All parameters
    are optional.
    The plugin should be declared in each struts-config file.
    - definitions-config: (optional)
    Specify configuration file names. There can be several comma
    separated file names (default: ?? )
    - moduleAware: (optional - struts1.1)
    Specify if the Tiles definition factory is module aware. If true
    (default), there will be one factory for each Struts module.
    If false, there will be one common factory for all module. In this
    later case, it is still needed to declare one plugin per module.
    The factory will be initialized with parameters found in the first
    initialized plugin (generally the one associated with the default
    module).
    true : One factory per module. (default)
    false : one single shared factory for all modules
    - definitions-parser-validate: (optional)
    Specify if xml parser should validate the Tiles configuration file.
    true : validate. DTD should be specified in file header (default)
    false : no validation

    Paths found in Tiles definitions are relative to the main context.
    -->
    <plug-in className="org.apache.struts.tiles.TilesPlugin" >
        <set-property property="definitions-config" value="/WEB-INF/tiles-defs.xml" />      
        <set-property property="moduleAware" value="true" />
    </plug-in>
    
    <!-- ========================= Validator plugin ================================= -->
    <plug-in className="org.apache.struts.validator.ValidatorPlugIn">
        <set-property
            property="pathnames"
            value="/WEB-INF/validator-rules.xml,/WEB-INF/validation.xml"/>
    </plug-in>
  
</struts-config>
